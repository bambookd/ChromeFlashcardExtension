node_modules/
backend/node_modules/
backend/data/
backend/exports/
*.log
.env

# Build artifacts (see REPO-002)
*.zip
backend/dist/
infra/.aws-sam/
infra/samconfig.toml

# Experimental duplicate copy, not source of truth (see REPO-001)
ChromeFlashCardExtension-test-aws-clean/

# Báo cáo thực tập cá nhân, không liên quan tới project
fcj-internship-report/

# Giá trị hạ tầng thật của từng người. Repo là public nên docs/ dùng placeholder.
infra/env.local.md
env.local.md

# File export do người dùng tải về, không phải mã nguồn.
# Mẫu CSV dùng để test nằm trong docs/ và vẫn được commit.
/flashcards-*.csv
